namespace LCAP.HRMS.Domain.Enums;

// Placeholder values: confirm lifecycle rules when the employee module is designed.
public enum EmployeeStatus
{
    Unknown = 0,
    Active = 1,
    Inactive = 2,
    OnLeave = 3,
    Terminated = 4
}
